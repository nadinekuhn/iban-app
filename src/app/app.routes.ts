import { Routes } from '@angular/router';
import {IbanValidierenComponent} from "./iban/iban-validieren/iban-validieren.component";

export const routes: Routes = [
    {
        path: '',
        component: IbanValidierenComponent
    }
];
